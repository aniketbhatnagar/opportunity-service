# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /v1/prioritization/getTopPerformanceOpps/:scoreType     com.accent.prioritization.controllers.PrioritizationController.getTopPerformanceOpps(scoreType: com.accent.prioritization.domain.ScoreTypes.Value, limit: Int = 10)
